<Page
    x:Class="LightServer.Pages.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LightServer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Image x:Name="albumArtImage" Height="300" Width="300" Margin="0,0,0,30" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <ProgressBar x:Name="playingTime" Value="0" ShowPaused="False" ShowError="False" MaxWidth="300" Width="200" />
            <TextBlock x:Name="playingTimeText" Text="00:00 / 00:00" Margin="5,0,0,0"/>
        </StackPanel>
        <TextBlock x:Name="titleText" Text="Unknown title" Margin="0,15,0,0" />
    </StackPanel>
</Page>
